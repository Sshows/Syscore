# Syscore

Каркас сайта на Next.js (App Router) + TypeScript + Tailwind CSS.

## Локальный запуск

Нужны Node.js 20+ и npm.

```bash
npm install
npm run dev
```

Сборка:

```bash
npm run build
```

Контент и секции сайта появятся на следующих этапах.
