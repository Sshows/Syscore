import { ButtonLink } from "@/components/ui/Button";
import { CyberCore } from "@/components/ui/CyberCore";
import { PlanBadge } from "@/components/ui/PlanBadge";
import { Reveal } from "@/components/ui/Reveal";
import { siteContent } from "@/content/site-content";

export function Hero() { const { hero, company } = siteContent; return <section id="top" className="tech-grid relative isolate overflow-hidden border-b border-white/10"><div className="mx-auto grid min-h-[760px] max-w-6xl items-center gap-12 px-4 py-24 sm:px-6 lg:grid-cols-[1fr_.9fr]"><Reveal><PlanBadge label={hero.eyebrow} /><p className="mt-8 text-xs font-bold tracking-[.3em] text-mint">SYSTEM SECURITY CORE</p><h1 className="mt-5 max-w-3xl text-5xl font-semibold leading-[.94] tracking-[-.065em] text-white sm:text-7xl lg:text-8xl">{hero.title}</h1><p className="mt-7 max-w-lg text-base leading-8 text-white/62">{hero.description}</p><div className="mt-10 flex flex-wrap gap-3"><ButtonLink href={hero.ctaHref}>{hero.ctaLabel} →</ButtonLink><ButtonLink href={company.whatsappHref} target="_blank" rel="noreferrer" className="border border-white/20 bg-white/5 text-white shadow-none hover:bg-white/10">{hero.whatsappLabel}</ButtonLink></div><div className="mt-12 flex flex-wrap gap-x-6 gap-y-2 text-[10px] font-bold tracking-[.18em] text-white/42"><span>{hero.companyStatus}</span><span>{hero.registrationStatus}</span><span>SYSCORE / 001</span></div></Reveal><Reveal className="relative mx-auto w-full max-w-[530px]"><div className="tech-card relative overflow-hidden rounded-[2rem] p-5 sm:p-7"><div className="scan-line absolute right-0 left-0 h-24 bg-gradient-to-b from-transparent via-mint/10 to-transparent" /><div className="relative flex justify-between text-[10px] font-bold tracking-[.18em] text-white/45"><span>CORE / CONCEPT</span><span className="text-mint">VISUAL ONLY</span></div><CyberCore /><div className="relative grid gap-2 border-t border-white/10 pt-4 text-[10px] font-bold tracking-[.14em] text-white/45">{hero.interfaceMetadata.map((item) => <span key={item}>{item}</span>)}</div></div></Reveal></div></section>; }
