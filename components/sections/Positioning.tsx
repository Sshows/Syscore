import { Reveal } from "@/components/ui/Reveal";
import { siteContent } from "@/content/site-content";
const architecture = ["BUSINESS", "SECURITY", "SOC / THREAT INTELLIGENCE / INCIDENT RESPONSE", "PEOPLE", "EDUCATION"];
export function Positioning() { const { positioning } = siteContent; return <section id={positioning.id} className="mx-auto max-w-6xl px-4 py-20 sm:px-6 sm:py-28"><Reveal><p className="text-xs font-bold tracking-[.2em] text-mint">SYSTEM ARCHITECTURE</p><h2 className="mt-4 text-4xl font-semibold tracking-tight text-white sm:text-5xl">{positioning.title}</h2><p className="mt-5 max-w-3xl leading-8 text-white/60">{positioning.description}</p></Reveal><Reveal className="mt-12"><div className="tech-card relative overflow-hidden rounded-[2rem] p-5 sm:p-9"><div className="absolute top-0 bottom-0 left-1/2 w-px bg-gradient-to-b from-transparent via-mint/50 to-transparent" /><div className="relative grid gap-3">{architecture.map((label, index) => <div key={label} className="flex items-center gap-4"><span className="w-8 text-xs font-bold text-mint/60">0{index + 1}</span><div className="flex-1 rounded-xl border border-white/10 bg-black/15 px-5 py-4 text-center text-xs font-bold tracking-[.14em] text-white/80 sm:text-sm">{label}</div></div>)}</div></div></Reveal></section>; }
