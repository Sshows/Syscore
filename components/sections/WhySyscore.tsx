import { Reveal } from "@/components/ui/Reveal";
import { siteContent } from "@/content/site-content";
export function WhySyscore() { const { whySyscore } = siteContent; return <section className="mx-auto max-w-6xl px-4 py-20 sm:px-6 sm:py-28"><Reveal><p className="text-xs font-bold tracking-[.2em] text-mint">WHY SYSCORE</p><h2 className="mt-5 text-4xl font-semibold tracking-tight text-white sm:text-6xl">{whySyscore.title}</h2></Reveal><div className="mt-10 grid gap-4 md:grid-cols-2">{whySyscore.items.map((item) => <Reveal key={item.number}><article className="tech-card group relative overflow-hidden rounded-[2rem] p-7"><span className="absolute -top-8 right-5 text-9xl font-bold text-white/[.035]">{item.number}</span><p className="text-xs font-bold tracking-[.2em] text-mint">{item.number}</p><h3 className="mt-8 text-3xl font-semibold text-white">{item.title}</h3><p className="mt-2 text-white/60">{item.text}</p></article></Reveal>)}</div><Reveal className="mt-8"><div className="flex flex-wrap items-center gap-3 text-xs font-bold tracking-[.14em] text-white/65">{whySyscore.chain.map((item, index) => <span key={item} className="flex items-center gap-3"><span>{item.toUpperCase()}</span>{index < whySyscore.chain.length - 1 ? <span className="text-mint">→</span> : null}</span>)}</div></Reveal></section>; }
