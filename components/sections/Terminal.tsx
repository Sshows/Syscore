import { Reveal } from "@/components/ui/Reveal";
import { siteContent } from "@/content/site-content";
export function Terminal() { const { interface: copy } = siteContent; return <section className="mx-auto max-w-6xl px-4 py-8 sm:px-6"><Reveal><div className="terminal-panel rounded-3xl p-6 sm:p-8"><div className="flex justify-between border-b border-white/10 pb-4 text-xs font-bold tracking-[.16em]"><span className="text-mint">{copy.terminalTitle}</span><span className="text-white/40">CONCEPT MODE</span></div><div className="mt-6 space-y-2 font-mono text-sm text-white/60">{copy.terminalLines.map((line, index) => <p className="terminal-line" style={{ animationDelay: `${index * 130}ms` }} key={line}><span className="text-mint">&gt;</span> {line}</p>)}</div><p className="mt-7 border-t border-white/10 pt-4 text-xs font-bold tracking-[.18em] text-white/45">STATUS: <span className="text-mint">{copy.terminalStatus}</span></p></div></Reveal></section>; }
